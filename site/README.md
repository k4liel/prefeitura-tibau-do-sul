# Pasta descontinuada

Esta pasta foi mantida apenas por compatibilidade local.

Use:

- `legacy/painel-estatico/site/` para o painel estatico antigo
- `backend/` para o sistema Django modular (ativo)
